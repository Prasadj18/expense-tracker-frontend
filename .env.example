# Base URL for the backend API
# In development the Vite proxy handles /api, so this can be empty
# In production point to your Render backend URL
VITE_API_BASE_URL=https://expense-tracker-backend-fs20.onrender.com
